import React from 'react';
import styles from './styles.module.scss';

const Navbar = () => {
  return (
    <div className={styles.navbar}>
      <div>
        <h1 className={styles.title}>Kudo Card Generator</h1>
      </div>

    </div>
  );
};

export default Navbar;
